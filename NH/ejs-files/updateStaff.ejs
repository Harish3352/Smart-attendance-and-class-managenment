<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Regester</title>
    <link rel="stylesheet" href="/regester.css">
</head>
<body>
    <div class="main">
        <div class="form">
            <h1 class="Addstd">Update Staff</h1>
            <form method="post" id="myform">
                <div class="stdName">
                    <label for="name">Lecture Name :</label>
                    <input type="text" name="name" id="name" value="<%= result[0].name %>" required>
                </div>
                <div class="stdUsn">
                    <label for="usn">Lecture id :</label>
                    <input type="text" name="id" id="usn" value="<%= result[0].id %>" required>
                </div>
                <div class="stdPassword">
                    <label for="password">Password :</label>
                    <input type="password" name="password" id="password" value="<%= result[0].password %>" required>
                </div>
                <div class="stdPhone">
                    <label for="phone">Phone no :</label>
                    <input type="number" name="phone" id="phone" value="<%= result[0].phone %>" required>
                </div>
                <div class="stdEmail">
                    <label for="email">Email id :</label>
                    <input type="email" name="email" id="email" value="<%= result[0].email %>" required>
                </div>
                <div class="type">
                    <select id="staffType" name="staffType" required>
                        <option value="Teaching">Teaching</option>
                        <option value="Non-teaching">Non-Teaching</option>
                    </select>
                </div>
            </form>
            <div class="button">
                <button type="submit" form="myform">Submit</button>
            </div>
        </div>
    </div>
</body>
</html>