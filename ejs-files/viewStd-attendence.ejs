<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/viewStd-attendence.css">
</head>
<body>
    <div class="main">
        <nav>
            <div class="logo">
                <div class="round"><%= adminName[0] %></div>
                <div class="text">Well come <br><%= adminDept %> / Admin : <%= adminName %></div>
            </div>
            <a href="/logout" class="button">Logout</a>
        </nav>
        <div class="content">
            <div class="heading">
                <h2><%= result[0].subname %> - ATTENDENCE</h2>
            </div>
            <div class="contentBox">
                <div class="left">
                    <div class="present">
                        <div class="presentBox" style="height:<%=Math.floor((Prst.length/(Prst.length + Arst.length))*100)%>%;"></div>
                        <label for="presentBox">Present</label>
                    </div>
                    <div class="gap"></div>
                    <div class="absent">
                        <div class="absentBox" style="height:<%=Math.floor((Arst.length/(Prst.length + Arst.length))*100)%>%;"></div>
                        <label for="absentBox">Absent</label>
                    </div>
                </div>
                <div class="right">
                    <h3>
                        Total class : <b><%= Prst.length + Arst.length %></b><br>
                        Present : <b><%= Prst.length %></b><br>
                        Absent : <b><%= Arst.length %></b><br>
                    </h3>
                    <span>AVG : <%= Math.floor((Prst.length/(Prst.length + Arst.length))*100)%>%</span>
                </div>
            </div>
        </div>
    </div>
</body>
</html>